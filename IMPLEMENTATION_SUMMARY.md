# 深蹲闯关游戏功能实现总结

## 功能概述
实现了在网页中点击"开始"按钮，打开摄像头并进行深蹲计数的功能：
1. 点击"开始"按钮打开摄像头
2. 实时显示深蹲检测处理后的图像
3. 计数达到5时自动停止
4. 显示"闯关成功"界面
5. 点击确认后回到开始界面

## 修改的文件

### 1. server/app.py
**主要修改内容：**
- 添加了深蹲检测相关的导入（SquatDetector）
- 添加了深蹲挑战的WebSocket事件处理：
  - `handle_start_squat_challenge`: 开始深蹲挑战
  - `handle_video_frame`: 接收视频帧并进行深蹲检测
  - `handle_stop_squat_challenge`: 停止深蹲挑战
- 在`init_managers()`中初始化深蹲检测器
- 添加了全局变量 `squat_detector` 和 `squat_target_count`

### 2. web/index.html
**主要修改内容：**
- 重新设计了页面结构，包含4个主要屏幕：
  - `#loading-screen`: 加载画面
  - `#start-screen`: 开始页面（显示规则和挑战按钮）
  - `#squat-screen`: 深蹲挑战页面（摄像头画面、计数显示）
  - `#success-screen`: 成功页面（闯关成功提示）
- 添加了系统状态指示器
- 添加了摄像头预览区域
- 添加了进度条和状态显示

### 3. web/js/app.js
**主要修改内容：**
- 重写了前端逻辑，实现以下功能：
  - WebSocket连接管理（`SocketManager`）
  - 页面切换管理（`ScreenManager`）
  - 深蹲挑战UI控制（`SquatUI`）
  - 摄像头启动和停止
  - 视频帧捕获和发送
  - 实时更新深蹲计数和进度
  - 系统状态检查（`SystemCheck`）

### 4. web/css/style.css
**主要修改内容：**
- 重新设计了样式，添加以下模块：
  - 加载画面样式
  - 开始页面样式（规则、按钮、状态指示器）
  - 深蹲挑战页面样式（摄像头画面、计数器、进度条）
  - 成功页面样式（闯关成功动画和统计）
  - Toast提示样式
  - 响应式设计（适配移动端）
  - 动画效果（脉冲、淡入、弹跳等）

## 使用方法

1. 确保服务器和检测模型已准备好
2. 打开浏览器访问 `http://localhost:5000`
3. 等待页面加载完成，检测器初始化
4. 点击"开始挑战"按钮
5. 允许浏览器访问摄像头
6. 面对摄像头，保持全身在画面内
7. 完成深蹲动作，实时计数将显示在画面中
8. 计数达到5次后，显示"闯关成功"
9. 点击"确认"按钮返回开始界面

## 技术特点

- 使用WebSocket实现实时视频流传输
- 服务器端使用MediaPipe进行姿态检测
- 使用KNN分类器进行深蹲姿态识别
- 状态机跟踪深蹲动作（站立->下蹲->站起）
- 客户端和服务器端双重验证确保计数准确
